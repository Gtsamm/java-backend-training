  server:
    port: 8080

  spring:
    datasource:
      url: "jdbc:mysql://localhost:3306/training?useSSL=false&serverTimezone=Asia/Shanghai&characterEncoding=UTF-8"
      username: root
      password: root123
      driver-class-name: com.mysql.cj.jdbc.Driver

  mybatis-plus:
    configuration:
      log-impl: org.apache.ibatis.logging.stdout.StdOutImpl     
    global-config:
      db-config:
        id-type: auto
        logic-delete-field: deleted
        logic-delete-value: 1
        logic-not-delete-value: 0